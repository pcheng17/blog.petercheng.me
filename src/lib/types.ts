export type PostMetadataProps = {
  pubDate: string;
  tags: string[];
};

export type BlogPostItemProps = {
  title: string;
  pubDate: string;
  tags: string[];
  description: string;
  path: string;
};
